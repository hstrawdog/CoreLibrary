<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.hqq.coreapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".APP"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.ToolBarActivity"
            android:label="设置标题栏" />
        <activity android:name=".ui.activity.IFragmentActivity" />
        <activity android:name=".ui.activity.LoadMoreActivity" />
        <activity
            android:name=".ui.activity.DefActivity"
            android:label="默认Activity" />
        <activity android:name=".ui.activity.RcActivity" />
        <activity android:name=".ui.activity.SearchBarActivity" />
        <activity android:name=".ui.activity.BannerActivity" />
        <activity android:name=".ui.activity.DefImgActivity" />
        <activity android:name=".ui.activity.PermissionActivity"></activity>
    </application>

</manifest>